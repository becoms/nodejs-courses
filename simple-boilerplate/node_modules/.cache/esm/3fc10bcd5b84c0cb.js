let mongoose;_5b8‍.x([["Shoes",()=>Shoes]]);_5b8‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

const ShoesSchema = new mongoose.Schema(
  {
    name: {
      type: String,
      required: true,
    },
    status: {
      type: String,
      required: true,
      default: "active",
      enum: ["active", "archived"],
    },
    color: {
      type: [String],
    }
  },
  { timestamps: true }
);

ShoesSchema.path("createdAt").immutable(true);

       const Shoes = mongoose.model("Shoes", ShoesSchema);
